import { OnInit } from '@angular/core';
import { NgProgress } from '../../services/ng-progress.service';
import { Observable } from 'rxjs/Observable';
export interface ProgressBarState {
    containerClasses: any;
    progressStyles: any;
    meteorStyles?: any;
    spinnerClasses?: any;
    spinnerStyles?: any;
}
export declare class NgProgressBarComponent implements OnInit {
    progress: NgProgress;
    ease: string;
    speed: number;
    meteor: boolean;
    spinner: boolean;
    spinnerPosition: string;
    direction: string;
    thick: string;
    color: string;
    state$: Observable<ProgressBarState>;
    constructor(progress: NgProgress);
    ngOnInit(): void;
    private containerClasses(state);
    private progressBarStyles(state);
    /**
     * Styles for progressbar tail
     */
    private meteorStyles();
    /**
     * Convert number to percent
     * @param n - State value
     */
    private toPercentage(n);
    /**
     * Spinner direction
     */
    private spinnerClasses();
    /**
     * Spinner styles
     */
    private spinnerStyles();
}
